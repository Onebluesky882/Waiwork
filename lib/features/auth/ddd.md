🔑 สรุปการทำงาน

UI / UseCase → เรียก Repository

Repository Implementation → ดึงข้อมูลจาก DataSource

DataSource → คืน JSON / DB record

Repository → แปลง JSON → Model → Entity

Entity → UseCase / UI
